---

# **Вариант 6**

## **Расширение функциональности веб-приложения и разработка продвинутого RPA-сценария**

---

# **Кейс: «Автоматизация выдачи и возврата книг в библиотеке»**

---

## 📖 Ситуация

После запуска базового портала регистрации заявок компания **LibraryPro** столкнулась с необходимостью управления жизненным циклом выдачи книг:

* необходимо подтверждать заявки после проверки доступности книги;
* отмечать возврат книг читателями;
* отслеживать статус выполнения заявок.

В связи с этим было принято решение:

* расширить функциональность веб-приложения;
* реализовать более сложный сценарий автоматизации с использованием **Puzzle RPA**.

---

# **Задание 1. Расширение функциональности веб-приложения**

Необходимо доработать существующее веб-приложение, добавив новые элементы управления и логику обработки заявок.

**Каждый элемент интерфейса должен иметь уникальный `id`**

---

## 🔘 Новые кнопки управления

---

### `Approve Loan`

**Назначение:**
Подтверждение выдачи книги читателю

**Логика:**

```json
{ "status": "Issued" }
```

**Требования:**

* доступна только если `Status = "Requested"`
* при нажатии изменяет статус выбранной записи на `"Issued"`

---

### `Return Book`

**Назначение:**
Отметка возврата книги в библиотеку

**Логика:**

```json
{ "status": "Returned" }
```

**Требования:**

* доступна только если `Status = "Issued"`
* при нажатии изменяет статус выбранной записи на `"Returned"`

---

## ⚙️ Управление состоянием элементов (`disabled`)

---

### Кнопки `Approve Loan`, `Return Book`

Должны быть:

* `disabled = true`, если:

  * ни одна строка не выбрана

* `disabled = false`, если:

  * выбрана строка таблицы

---

### Дополнительные условия

* `Approve Loan` должна быть неактивна, если:

```json
{ "status": "Issued" }
или
{ "status": "Returned" }
```

* `Return Book` должна быть неактивна, если:

```json
{ "status": "Requested" }
или
{ "status": "Returned" }
```

---

## 🔘 Детализация кнопок управления

---

### `Approve Loan`

**Назначение:**
Подтверждение выдачи книги после проверки доступности.

**Бизнес-смысл:**
Книга зарезервирована и выдана читателю.

---

**Когда используется:**

* библиотекарь проверил наличие книги;
* заявка соответствует правилам выдачи.

---

**Алгоритм работы:**

1. Пользователь выбирает строку в таблице.
2. Проверяется текущий статус заявки:

   * если `Status ≠ Requested` → операция запрещена;
   * если `Status = Requested` → операция разрешена.
3. При нажатии кнопки:

   * статус изменяется на:

```json
{ "status": "Issued" }
```

4. Таблица обновляется (с задержкой, если реализована асинхронность).

---

**Ограничения:**

* нельзя повторно выдать уже обработанную заявку;
* кнопка должна быть `disabled`, если:

  * не выбрана строка;
  * статус уже `Issued` или `Returned`.

---

### `Return Book`

**Назначение:**
Отметка возврата книги в библиотеку.

**Бизнес-смысл:**
Книга возвращена читателем и доступна для следующей выдачи.

---

**Когда используется:**

* читатель вернул книгу в срок;
* книга проверена на целостность.

---

**Алгоритм работы:**

1. Выбирается строка таблицы.
2. Проверяется статус:

   * допускается только `Issued`.
3. При нажатии:

   * статус изменяется на:

```json
{ "status": "Returned" }
```

4. Таблица обновляется.

---

**Ограничения:**

* нельзя вернуть книгу, которая не была выдана;
* кнопка `disabled`, если:

  * нет выбранной строки;
  * статус уже обработан.

---

## 🔍 Фильтрация заявок

Необходимо добавить блок фильтрации:

### `Filter by Category`

**Значения:**

* `All`
* `Programming`
* `Databases`
* `Networking`
* `AI`

---

### Логика

* при выборе значения отображаются только соответствующие записи
* фильтрация выполняется без перезагрузки страницы

---

## 🔍 Детализация фильтрации `Filter by Category`

---

### 📌 Назначение

Фильтр позволяет отображать заявки в зависимости от категории книги.

---

## 📊 Значения категорий и их смысл

| Категория | Значение | Когда используется |
| --------- | -------- | ------------------ |
| `Programming` | Книги по программированию | заявки на изучение языков |
| `Databases` | Книги по базам данных | заявки на проектирование БД |
| `Networking` | Книги по сетевым технологиям | заявки на изучение сетей |
| `AI` | Книги по искусственному интеллекту | заявки на машинное обучение |

---

## ⚙️ Алгоритм работы фильтра

---

### 1. Выбор значения

Пользователь выбирает значение в выпадающем списке:

```
Filter by Category
```

---

### 2. Обработка выбора

#### Если выбрано:

### `All`

* отображаются все записи без фильтрации

---

### `Programming`

* отображаются только записи:

```json
{ "category": "Programming" }
```

---

### `Databases`

* отображаются только:

```json
{ "category": "Databases" }
```

---

### `Networking`

* отображаются только:

```json
{ "category": "Networking" }
```

---

### `AI`

* отображаются только:

```json
{ "category": "AI" }
```

---

### 3. Применение фильтра

* фильтрация выполняется:

  * без перезагрузки страницы;
  * на стороне клиента (или сервера — опционально);
* обновление таблицы происходит с задержкой (имитация реального API).

---

### 4. Поведение при отсутствии данных

Если записи отсутствуют:

* отображается сообщение в текстовой метке:

```
No requests found
```

---

### 5. Взаимодействие с другими элементами

---

#### ✔ После изменения статуса

* запись может "исчезнуть" из текущего фильтра
  (например, была `Requested`, стала `Issued`)

---

#### ✔ После возврата

* таблица автоматически обновляется с учётом текущего фильтра

---

## 🎯 Итоговая логика состояний

```text
Создание заявки → Requested
            ↓
         Issued
            ↓
        Returned
```

---

## 🔎 Поиск

### `Search by Book`

**Назначение:**
Поиск заявки по названию книги

**Требования:**

* поиск выполняется в реальном времени
* частичное совпадение допустимо

---

## ⏳ Дополнительная асинхронность

Необходимо реализовать:

* задержку обновления таблицы после изменения статуса (1–2 сек)
* задержку при возврате книги
* имитацию загрузки (`Loading...`)

---

# 📥 Входные данные (CSV / Excel)

Файл: `book_requests.xlsx`

| Book Title | Reader Name | Category | Duration |
| ---------- | ----------- | -------- | -------- |
| Python Crash Course | Ivan Petrov | Programming | 14 days |
| Database Design | Anna Smirnova | Databases | 30 days |
| Network Fundamentals | Dmitry Orlov | Networking | 7 days |
| AI Basics | Olga Ivanova | AI | 14 days |
| Clean Code | Sergey Kuznetsov | Programming | 30 days |
| SQL for Beginners | Elena Volkova | Databases | 14 days |
| Cloud Networking | Pavel Morozov | Networking | 30 days |
| Machine Learning | Irina Lebedeva | AI | 30 days |
| Advanced Python | Maxim Kozlov | Programming | 7 days |
| NoSQL Databases | Natalia Sokolova | Databases | 14 days |

---

# **Задание 2. Разработка продвинутого RPA-сценария**

Необходимо реализовать процесс автоматизации, обрабатывающий заявки в веб-приложении.

---

## 🤖 Алгоритм работы робота

### 1. Подготовка

* открыть браузер
* перейти на сайт
* дождаться загрузки страницы

---

### 2. Добавление заявок

Для каждой записи:

* заполнить форму
* проверить `Submit Request.disabled`
* при необходимости исправить данные
* отправить форму
* дождаться сообщения об успехе в текстовой метке

---

### 3. Выдача всех книг

После добавления:

* для каждой записи в таблице:

  * выбрать строку
  * проверить доступность `Approve Loan`
  * изменить статус на:

```json
{ "status": "Issued" }
```

---

### 4. Возврат книг

* выбрать 2 записи со статусом `Issued`

* для каждой выбранной записи:

  * нажать `Return Book`
  * убедиться, что статус изменился на `"Returned"`

---

### 5. Использование фильтра

* выбрать `Filter by Category = Programming`
* убедиться, что отображаются только нужные записи

---

### 6. Поиск

* выполнить поиск по названию книги (например: `"Python"`)
* проверить корректность результата

---

### 7. Проверка итогового состояния

* проверить:

  * все заявки добавлены (10 записей)
  * все записи имеют статус `"Issued"` или `"Returned"`
  * 2 записи имеют статус `"Returned"`

---

# 📊 Ожидаемый результат

---

## ✔ После выполнения

* добавлены все записи
* статусы обновлены:

```json
10 записей → Issued
2 записи → Returned
```

---

## 📋 Пример итоговой таблицы

| ID | Book | Category | Status |
| -- | ---- | -------- | ------ |
| 1 | Python Crash Course | Programming | Issued |
| 2 | Database Design | Databases | Returned |
| 3 | Network Fundamentals | Networking | Issued |

---

# 🔎 Требования к корректности выполнения

---

### ✔ Веб-приложение

* корректно реализованы:

  * фильтрация
  * поиск
  * управление `disabled`

---

### ✔ RPA-сценарий

* корректно работает с динамическими элементами
* использует ожидания (`Wait`)
* обрабатывает фильтрацию и поиск

---

### ✔ Данные

* все записи обработаны
* статусы корректны
* отсутствуют дубликаты

---

# 🧾 Логирование

Необходимо реализовать расширенное логирование

---

## 📌 Должно фиксироваться

* запуск процесса
* добавление записей
* выдача книг
* возврат книг
* применение фильтра
* поиск
* ошибки

---

## 📄 Пример

```json
{
  "time": "2026-04-03 11:45:02",
  "step": "ApproveLoan",
  "book": "Python Crash Course",
  "new_status": "Issued",
  "result": "success"
}
```

---

# 🧩 Требования к модульности

Рекомендуется выделить подпроцессы:

* `OpenBrowser`
* `FillForm`
* `SubmitApplication`
* `ApproveLoan`
* `ReturnBook`
* `ApplyFilter`
* `Search`
* `HandleModal`
* `ValidateResults`

---
